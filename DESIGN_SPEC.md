# Design Spec — Gift 2026 Event Finance Dashboard

เอกสารนี้เป็นสเปกสำหรับงานพัฒนา Dashboard หน้าเดียว  
**Tech stack ที่กำหนด:** HTML + CSS + Vanilla JavaScript  
**สถานะ:** ออกแบบเท่านั้น ยังไม่ลงมือเขียนโค้ด

- **หน้าตา:** อ้างอิง screenshot เว็บ Dashboard ที่อัปโหลด (KPI 3 การ์ด + กราฟแท่ง 2 อัน)
- **แหล่งข้อมูล:** Google Sheets ชีต `01_Dashboard` (ไม่ใช้ mock data)

---

## 1. วัตถุประสงค์

สร้างหน้า Dashboard การเงินของงาน Gift โรงเรียนไตรพัฒน์ เพื่อให้ผู้ใช้เห็นภาพรวมรายรับ รายจ่าย และยอดคงเหลือ พร้อมกราฟแยกประเภท ภายในหน้าเดียว

ข้อมูลทั้งหมดอ่านจาก Google Spreadsheet ที่ทีมการเงินใช้เป็น Single Source of Truth  
ผู้ใช้หลักคือทีมจัดงาน / PTA / Head ทีม ที่ดูยอดสรุป ไม่ได้กรอกข้อมูลเอง

ไม่มีระบบ login และไม่มี backend ของตัวเองในรอบนี้ — หน้าเว็บดึงชีตสาธารณะฝั่ง client แล้วเรนเดอร์

---

## 2. ขอบเขตงาน (Scope)

### ทำในรอบนี้

- หน้า Dashboard หน้าเดียว เปิดด้วยไฟล์ HTML โดยตรง
- Header: ชื่อ Dashboard
- การ์ดสรุป 3 ใบ: รายรับ / รายจ่าย / คงเหลือ — อ่านจาก Section A ของชีต
- กราฟแท่งซ้าย: รายรับแยกประเภท — อ่านจาก Section B (`Income by Source`)
- กราฟแท่งขวา: รายจ่ายแยกตามทีม — อ่านจาก Section C (`Expense by Team`)
- ดึงข้อมูลจาก Google Sheets ชีต `01_Dashboard` ตาม cell map ในข้อ 7
- แปลงข้อความตัวเลขในเซลล์เป็น number ก่อนแสดงผล

### ไม่ทำในรอบนี้

- Backend, database, Google Service Account
- Login / role / permission
- หน้าเพิ่ม-แก้ไขรายการเงิน (กรอกที่ชีต `02_Income` / `03_Expense` เท่านั้น)
- อ่านชีต `02_Income`, `03_Expense`, `04_Team_Budget`, `05_Master_Data`
- กรองช่วงวันที่ฝั่งเว็บ (ชีต `01_Dashboard` เป็นยอดสรุปทั้งก้อนแล้ว ไม่มีรายการย่อยครบสำหรับกรอง)
- Export PDF / Excel
- Dark mode
- Mobile app

### ตัวกรองวันที่

ปุ่ม `เลือกช่วงวันที่` ใน mock เว็บ **ยังไม่ทำงานในรอบนี้**

เหตุผล: ยอดใน `01_Dashboard` คำนวณมาแล้วทั้งกอง ไม่มีวันที่ต่อรายการใน Section A–D  
ถ้าต้องการกรองวันที่ในรอบถัดไป ให้อ่านรายการจาก `02_Income` และ `03_Expense` แล้วคำนวณใหม่

พฤติกรรม UI รอบนี้: แสดงปุ่มไว้ตาม mock แต่กดแล้วขึ้นข้อความ `แสดงยอดทั้งกองจากชีต Dashboard — ยังไม่กรองวันที่`

---

## 3. โครงสร้างหน้า (Layout)

หน้าเป็นแนวตั้ง 3 โซน กว้างเต็มจอ มี padding รอบด้าน

```
+------------------------------------------------------------------+
|  Gift 2026 Event Finance Dashboard          [ เลือกช่วงวันที่ ▾ ] |
+------------------------------------------------------------------+
|  รายรับ              รายจ่าย              คงเหลือ                 |
|  [ Total Income ]    [ Total Expense ]    [ Current Balance ]    |
|  [    50,016    ]    [     24.5K     ]    [     25,516      ]    |
+------------------------------------------------------------------+
|  รายรับแยกประเภท                    รายจ่ายแยกตามทีม              |
|  [ bar chart: Income by Source ]    [ bar chart: Expense by Team ]|
+------------------------------------------------------------------+
```

ตัวเลขในโครงด้านบนคือค่าปัจจุบันจากชีต ณ วันที่อ่านสเปกนี้ ไม่ hardcode ในโค้ดจริง — ต้องมาจาก cell ที่กำหนด

### Grid

| โซน | คอลัมน์ (desktop ≥ 1024px) | ช่องว่าง |
|------|----------------------------|---------|
| Header | 2 คอลัมน์: title ซ้าย / date picker ขวา | — |
| KPI | 3 คอลัมน์เท่ากัน | 24px |
| Charts | 2 คอลัมน์เท่ากัน | 24px |

### ระยะห่าง

| รายการ | ค่า |
|--------|-----|
| Padding หน้า | 32px 40px |
| ระยะระหว่าง header กับ KPI | 28px |
| ระยะระหว่าง KPI กับ charts | 32px |
| ระยะระหว่าง label ภาษาไทยกับกล่อง | 10px |
| Border radius การ์ด / ปุ่ม / กราฟ | 12px |

### Breakpoints

| จอ | พฤติกรรม |
|----|----------|
| ≥ 1024px | ตาม mock: KPI 3 คอลัมน์, กราฟ 2 คอลัมน์ |
| 768–1023px | KPI 3 คอลัมน์ยังอยู่, กราฟเรียงซ้อน 1 คอลัมน์ |
| < 768px | Header ซ้อนกัน, KPI 1 คอลัมน์, กราฟ 1 คอลัมน์, date picker เต็มความกว้าง |

---

## 4. Visual Design

หน้าตาเว็บยังอ้างอิง screenshot Dashboard (การ์ดสีทึบ + กราฟแท่ง) ไม่ได้ลอกสีของ Google Sheets

### 4.1 สีพื้นและผิว

| Token | Hex | ใช้กับ |
|-------|-----|--------|
| `--bg-page` | `#F4F6F8` | พื้นหลังหน้า (เทาอมฟ้าอ่อน) |
| `--bg-card` | `#FFFFFF` | การ์ดกราฟ, ปุ่ม date picker |
| `--text-primary` | `#1A1A1A` | หัวข้อหลัก, ตัวเลขเข้ม |
| `--text-secondary` | `#6B7280` | ป้ายเล็ก, แกนกราฟ, hint |
| `--text-on-dark` | `#FFFFFF` | ข้อความบนการ์ดสีเข้ม |
| `--border` | `#E5E7EB` | เส้นขอบปุ่ม date picker |
| `--grid-line` | `#EEF0F2` | เส้น grid แนวนอนในกราฟ |
| `--shadow` | `0 1px 3px rgba(16,24,40,0.08)` | การ์ด KPI และกราฟ |

### 4.2 สีการ์ด KPI

| การ์ด | พื้นหลัง | ข้อความ |
|-------|----------|---------|
| รายรับ (Total Income) | `#22A45A` เขียว | ขาว |
| รายจ่าย (Total Expense) | `#F5C518` เหลืองอำพัน | ดำ `#1A1A1A` |
| คงเหลือ (Current Balance) | `#7ED6E8` ฟ้าอ่อน | ดำ `#1A1A1A` |

เหตุผลที่รายจ่ายใช้ตัวอักษรสีเข้ม: พื้นเหลืองตัดกับขาวไม่พอ อ่านยาก

### 4.3 สีแท่งกราฟ — รายรับแยกประเภท (Section B)

เรียงตามลำดับแถวในชีต `A10:A15` ไม่เรียงตามความสูงแท่ง

| ลำดับ | หมวด (คอลัมน์ A) | Hex |
|-------|------------------|-----|
| 1 | PTA Contribution | `#3B6EF5` |
| 2 | Sponsorship | `#F5A524` |
| 3 | Workshop Revenue | `#8B5CF6` |
| 4 | Donations | `#84CC16` |
| 5 | Auction Proceeds | `#22D3EE` |
| 6 | Other Income | `#FACC15` |

ถ้าชีตเพิ่มหมวดใหม่ใน `A10:A15` ให้ต่อสีจากพาเลตเดียวกันตามลำดับ แล้ววนกลับสีแรกเมื่อเกิน 6 สี

### 4.4 สีแท่งกราฟ — รายจ่ายแยกตามทีม (Section C)

เรียงตามลำดับแถวในชีต `A20:A25`

| ลำดับ | ทีม (คอลัมน์ A) | Hex |
|-------|-----------------|-----|
| 1 | Gift Team | `#3B6EF5` |
| 2 | Shop Team | `#F5A524` |
| 3 | Venue & Safety Team | `#8B5CF6` |
| 4 | Public Relations Team | `#84CC16` |
| 5 | Workshop Team | `#22D3EE` |
| 6 | Activity Team | `#FACC15` |

**แก้จากสเปกเดิม:** mock เว็บใช้ชื่อหมวดค่าใช้จ่าย (Equipment, Materials, …) แต่หัวข้อกราฟคือ “รายจ่ายแยกตามทีม”  
รอบนี้ใช้ชื่อทีมจาก Section C ตามชีตจริง Section D (`Expense by Category`) เก็บ mapping ไว้ในข้อ 7.6 แต่ยังไม่แสดงบนหน้าเว็บ

### 4.5 Typography

ฟอนต์: ใช้ system stack ที่รองรับไทย

```
"Sarabun", "Noto Sans Thai", "Segoe UI", sans-serif
```

โหลด Sarabun จาก Google Fonts (น้ำหนัก 400, 600, 700)

| ระดับ | ขนาด | น้ำหนัก | สี | ใช้กับ |
|-------|------|---------|-----|--------|
| H1 | 28px / line 1.2 | 700 | `--text-primary` | ชื่อ Dashboard |
| Section label | 16px | 600 | `--text-primary` | รายรับ, รายจ่าย, คงเหลือ, ชื่อกราฟ |
| KPI caption | 13px | 400 | ตามพื้นการ์ด (ขาวหรือดำ 70%) | Total Income / Expense / Balance |
| KPI value | 36px | 700 | ตามพื้นการ์ด | ตัวเลขสรุป |
| Control | 14px | 400 | `--text-primary` | ปุ่มเลือกวันที่ |
| Legend / axis | 12px | 400 | `--text-secondary` | ป้ายกราฟ |

ตัวเลขใช้ tabular lining (`font-variant-numeric: tabular-nums`) เพื่อไม่ให้กระตุกตอนอัปเดตค่า

### 4.6 รูปแบบตัวเลข

ค่าที่อ่านจากชีตอาจเป็นข้อความ เช่น `50,016 บาท` ต้อง parse เป็น number ก่อน แล้วค่อยจัดรูปแบบตอนแสดงผล

| เงื่อนไข | รูปแบบ | ตัวอย่างจากชีตปัจจุบัน |
|----------|--------|-------------------------|
| ค่าบนการ์ด KPI ที่ < 10,000 | `#,###` | `4,250` |
| Total Income | `#,###` เต็มจำนวน | `50,016` |
| Total Expense ถ้า ≥ 10,000 | ย่อ `##.#K` | `24.5K` (จาก 24,500) |
| Current Balance | `#,###` เต็มจำนวน | `25,516` |
| ค่าบนการ์ด KPI ที่ ≥ 100,000 | `###.#K` | `120.4K` |
| แกน Y ของกราฟ | `0`, `10K`, `20K` หรือ `0`, `2K`, `4K` | ตามสเกลของชุดข้อมูล |
| Tooltip | `#,###` เต็มจำนวน | `25,000` |

สกุลเงินบนหน้าเว็บ **ไม่แสดง ฿ และไม่แสดงคำว่า บาท** (แม้เซลล์ในชีตจะมีคำว่า บาท)

---

## 5. Components

### 5.1 Header

- ซ้าย: ข้อความ `Gift 2026 Event Finance Dashboard` ไม่มีโลโก้  
  (ชื่อทางการในชีตอยู่ที่ `A1` ใช้เป็น `document.title` หรือ subtitle ได้ แต่ H1 ของหน้าเว็บคงตาม mock)
- ขวา: ปุ่ม `เลือกช่วงวันที่` พื้นขาว ขอบ `#E5E7EB` มุมโค้ง 8–12px สูงประมาณ 40px มีลูกศรชี้ลงด้านขวา
- คลิกแล้วแสดงข้อความตามข้อ 2 ไม่เปิด date picker จริงในรอบนี้

### 5.2 KPI Card

แต่ละใบมี 2 ส่วน:

1. **Label ด้านนอก** ภาษาไทย อยู่เหนือกล่องสี ไม่ได้อยู่ในกล่อง
2. **กล่องสี** เต็มความกว้างคอลัมน์ สูงประมาณ 88–96px padding 20px 24px
   - บรรทัดบน: caption ภาษาอังกฤษ
   - บรรทัดล่าง: ตัวเลขใหญ่

สามใบเรียงซ้ายไปขวา: รายรับ → รายจ่าย → คงเหลือ

ค่ามาจากเซลล์ในข้อ 7.3 **ห้ามคำนวณ balance เองในรอบนี้** ให้ใช้ค่า `E6` จากชีตโดยตรง  
(ชีตเป็นแหล่งความจริง หากคำนวณเองแล้วไม่ตรงกับสูตรในชีตจะงงตอนตรวจ)

ถ้า parse ไม่ได้ ให้แสดง `—`

### 5.3 Chart Card

กล่องขาว มีเงาบาง มุม 12px padding 20px 24px

โครงสร้างภายในจากบนลงล่าง:

1. หัวข้อภาษาไทย
2. Legend แนวนอน ชิดซ้าย ไอคอนสี่เหลี่ยม 10×10px ตามด้วยชื่อหมวด/ทีม ระยะห่างระหว่างรายการ 16px ถ้าไม่พอให้ขึ้นบรรทัดใหม่
3. พื้นที่กราฟแท่งแนวตั้ง

กราฟ:

- แท่งแนวตั้ง สีต่างกันตามหมวด/ทีม ไม่มีขอบ
- ความกว้างแท่งประมาณ 36–48px ระยะห่างระหว่างแท่งสม่ำเสมอ
- มุมบนของแท่งไม่โค้ง (ตาม mock เป็นสี่เหลี่ยมตัดตรง)
- เส้น grid แนวนอนบาง สี `--grid-line` ไม่มีเส้นแกนแนวตั้ง
- แกน Y ชิดซ้าย หน่วยย่อเป็น K
- แกน X เป็นชื่อจากชีต ถ้าชื่อยาวให้ตัดบรรทัดหรือเอียงเล็กน้อย ไม่ให้ทับกัน
- ไม่มีค่าตัวเลขบนยอดแท่งในสถานะปกติ ค่าเต็มอยู่ใน tooltip
- แถวที่ชื่อเป็น `TOTAL` **ห้ามวาดเป็นแท่ง**
- แถวที่จำนวนเป็น 0 ยังวาดแท่งสูง 0 และยังอยู่ใน legend

สเกลแกน Y: ปัดขึ้นเป็นจำนวนกลมที่อ่านง่าย (เช่น 10K, 20K, 50K หรือ 2K, 4K, 8K, 10K) ตามค่าสูงสุดของชุดข้อมูลที่อ่านได้

### 5.4 Empty / loading / error

| สถานะ | พฤติกรรม |
|--------|----------|
| กำลังดึงชีต | การ์ดและกราฟแสดงโครง ตัวเลขเป็น `—` มีข้อความเล็ก `กำลังโหลดข้อมูล` |
| ดึงสำเร็จ | เรนเดอร์ KPI + กราฟจาก cell map |
| ชีตไม่สาธารณะ / CORS / เครือข่ายล้ม | แสดงข้อความ `ไม่สามารถอ่าน Google Sheets ได้` ไม่ใส่ตัวเลขปลอม |
| เซลล์ว่างหรือ parse ไม่ได้ | ช่องนั้นเป็น `—` หรือแท่ง 0 ตามบริบท |
| CSV มีแถวน้อยกว่าที่ map ไว้ | ข้ามแถวที่ไม่มี ไม่ทำให้ทั้งหน้าพัง |

---

## 6. Interaction

ทั้งหมดทำงานฝั่ง client ไม่รีเฟรชหน้า

### 6.1 เลือกช่วงวันที่

รอบนี้ยังไม่กรองข้อมูล ดูข้อ 2

### 6.2 Tooltip กราฟ

เมื่อชี้แท่ง:

- กล่องเล็กพื้นขาว เงา ขอบบาง
- บรรทัด 1: ชื่อหมวดหรือทีม
- บรรทัด 2: จำนวนเต็มมี comma
- ไม่คลิกแท่งแล้วทำอะไรเพิ่มในรอบนี้

### 6.3 Legend

legend เป็นป้ายอ่านอย่างเดียว ไม่กดซ่อนซีรีส์

### 6.4 Keyboard

- Tab ไปที่ปุ่มวันที่ได้
- Escape ปิดข้อความ/popover ของปุ่มวันที่

### 6.5 Refresh ข้อมูล

ไม่มีปุ่มรีเฟรชในรอบนี้ โหลดชีตครั้งเดียวตอนเปิดหน้า  
ถ้าต้องการข้อมูลใหม่ ผู้ใช้รีโหลดหน้าเว็บ

---

## 7. แหล่งข้อมูล — Google Sheets

### 7.1 สมุดงาน

| รายการ | ค่า |
|--------|-----|
| ชื่อไฟล์ | Gift-Tripat-Finance |
| Spreadsheet ID | `1OQ8fC-vOglnPcy3aQKXOu743dX8YK2f9OovYg_8EjlA` |
| URL แก้ไข | [เปิดชีต](https://docs.google.com/spreadsheets/d/1OQ8fC-vOglnPcy3aQKXOu743dX8YK2f9OovYg_8EjlA/edit?gid=0#gid=0) |
| ชีตที่ใช้ในรอบนี้ | `01_Dashboard` |
| gid ของ `01_Dashboard` | `0` |
| สิทธิ์ที่ต้องตั้ง | Anyone with the link → Viewer (ไม่งั้น `fetch` จากเบราว์เซอร์ไม่ได้) |

แท็บอื่นในสมุดงานเดียวกัน **ห้ามดึงในรอบนี้** แต่บันทึกไว้เพื่อรอบถัดไป:

| ชีต | gid | บทบาท |
|------|-----|--------|
| `01_Dashboard` | `0` | ยอดสรุปที่หน้าเว็บอ่าน |
| `02_Income` | `919762478` | รายการเงินเข้า (กรอกโดยทีมการเงิน) |
| `03_Expense` | `1537394550` | รายการเงินออก |
| `04_Team_Budget` | `167654728` | งบต่อทีม |
| `05_Master_Data` | `1151140396` | รายการ dropdown |

ชีต `01_Dashboard` คำนวณอัตโนมัติจาก `02_Income` และ `03_Expense` หน้าเว็บไม่อ่านสองชีตนั้นโดยตรง

### 7.2 วิธีดึงข้อมูล (Vanilla JS)

ดึง CSV ของชีต `01_Dashboard` แล้ว map ตามแถว/คอลัมน์ด้านล่าง ไม่ใช้ Google Sheets API key

URL ที่ใช้:

```
https://docs.google.com/spreadsheets/d/1OQ8fC-vOglnPcy3aQKXOu743dX8YK2f9OovYg_8EjlA/gviz/tq?tqx=out:csv&gid=0
```

ทางเลือกสำรองถ้า URL ด้านบนล้ม:

```
https://docs.google.com/spreadsheets/d/1OQ8fC-vOglnPcy3aQKXOu743dX8YK2f9OovYg_8EjlA/export?format=csv&gid=0
```

ข้อกำหนดการ parse:

- แถวที่ 1 ของ CSV = แถวที่ 1 ของชีต (A1)
- คอลัมน์ A = index 0, B = 1, C = 2, D = 3, E = 4
- แผนที่ด้านล่างใช้ **แถว/คอลัมน์แบบ Google Sheets (1-based)** เช่น `B10` = แถว 10 คอลัมน์ B
- อย่าอ้างอิงด้วยชื่อหมวดอย่างเดียวเป็นหลัก เพราะลำดับแถวคือสัญญาของสเปกนี้ ชื่อใช้แสดงผล
- ข้ามแถวที่คอลัมน์ชื่อว่าง
- ข้ามแถวที่ชื่อเท่ากับ `TOTAL` (ไม่สนตัวพิมพ์) เมื่อสร้างกราฟ

### 7.3 การแปลงค่าในเซลล์

| รูปแบบในเซลล์ | วิธีแปลง | ผล |
|---------------|----------|-----|
| `50,016 บาท` | ลบ `บาท`, ลบ comma, `trim`, `Number(...)` | `50016` |
| `24,500` | ลบ comma | `24500` |
| `0` | number | `0` |
| ว่าง | ไม่มีค่า | `null` → แสดง `—` หรือข้ามแถว |
| serial วันที่ เช่น `46250` | Google serial (epoch `1899-12-30`) | `2026-08-16` |

สูตรวันที่:

```
date = 1899-12-30 + serial วัน
ตัวอย่าง: 46250 → 2026-08-16
```

ห้ามแสดง serial ดิบบนหน้าเว็บ

---

### 7.4 Section A — Financial Overview (KPI)

ช่วงที่เกี่ยวข้อง: แถว **4–6** คอลัมน์ **A, C, E**

| UI | Label ในชีต | เซลล์ label | เซลล์ค่า | ค่าตัวอย่างเมื่อเขียนสเปก |
|----|-------------|-------------|----------|---------------------------|
| การ์ดรายรับ | TOTAL INCOME | `A5` | `A6` | `50,016 บาท` → แสดง `50,016` |
| การ์ดรายจ่าย | TOTAL EXPENSE | `C5` | `C6` | `24,500 บาท` → แสดง `24.5K` |
| การ์ดคงเหลือ | CURRENT BALANCE | `E5` | `E6` | `25,516 บาท` → แสดง `25,516` |

เซลล์อ้างอิง (ไม่ต้องแสดง แต่ใช้ตรวจ):

| เซลล์ | เนื้อหา |
|-------|---------|
| `A1` | ชื่องาน `งาน Gift โรงเรียนไตรพัฒน์ — เงินกองกลาง` |
| `A2` | คำอธิบายแหล่งข้อมูล |
| `A4` | หัวข้อ `A. Financial Overview` |

หมายเหตุ: การ์ดในชีตอาจดูเหมือนกินคอลัมน์คู่ (A–B, C–D, E–F) แต่ค่าที่ export เป็น CSV อยู่ที่ **A6, C6, E6** ให้อ่านสามเซลล์นี้เท่านั้น อย่าอ่าน B6 / D6 / F6

```
แถว 5:  A5 TOTAL INCOME     C5 TOTAL EXPENSE     E5 CURRENT BALANCE
แถว 6:  A6 50,016 บาท       C6 24,500 บาท        E6 25,516 บาท
```

---

### 7.5 Section B — Income by Source → กราฟซ้าย (รายรับแยกประเภท)

ช่วงข้อมูล: **`A10:B15`**  
หัวตาราง: `A9` = `Source`, `B9` = `Amount`  
แถวรวม: **`A16:B16`** ชื่อ `TOTAL` — ใช้ตรวจยอด ไม่วาดแท่ง

| แถว | เซลล์ชื่อ (A) | เซลล์จำนวน (B) | ค่าตัวอย่าง (B) |
|-----|---------------|-----------------|-----------------|
| 10 | `A10` PTA Contribution | `B10` | 25,000 |
| 11 | `A11` Sponsorship | `B11` | 8,000 |
| 12 | `A12` Workshop Revenue | `B12` | 4,700 |
| 13 | `A13` Donations | `B13` | 5,000 |
| 14 | `A14` Auction Proceeds | `B14` | 6,116 |
| 15 | `A15` Other Income | `B15` | 1,200 |
| 16 | `A16` TOTAL | `B16` | 50,016 |

กฎอ่าน:

1. วนแถว 10 ถึง 15
2. ถ้า `A{n}` ว่าง ให้ข้าม
3. ถ้า `A{n}` เป็น `TOTAL` ให้หยุด
4. parse `B{n}` เป็น number แล้วใช้เป็นความสูงแท่ง
5. ตรวจทางเลือก: `sum(B10:B15)` ควรเท่ากับ `B16` และใกล้ `A6` — ถ้าไม่เท่า ยังเรนเดอร์กราฟจากแถว 10–15 แต่ไม่ overwrite การ์ด KPI

---

### 7.6 Section C — Expense by Team → กราฟขวา (รายจ่ายแยกตามทีม)

ช่วงข้อมูล: **`A20:B25`**  
หัวตาราง: `A19` = `Team`, `B19` = `Total Payment`  
แถวรวม: **`A26:B26`** ชื่อ `TOTAL` — ใช้ตรวจยอด ไม่วาดแท่ง  
หัวข้อโซน: `A18` = `C. Expense by Team`

| แถว | เซลล์ชื่อ (A) | เซลล์จำนวน (B) | ค่าตัวอย่าง (B) |
|-----|---------------|-----------------|-----------------|
| 20 | `A20` Gift Team | `B20` | 3,300 |
| 21 | `A21` Shop Team | `B21` | 3,900 |
| 22 | `A22` Venue & Safety Team | `B22` | 9,200 |
| 23 | `A23` Public Relations Team | `B23` | 2,000 |
| 24 | `A24` Workshop Team | `B24` | 2,700 |
| 25 | `A25` Activity Team | `B25` | 3,400 |
| 26 | `A26` TOTAL | `B26` | 24,500 |

กฎอ่านเหมือน Section B โดยวนแถว 20 ถึง 25 หยุดที่ `TOTAL`

---

### 7.7 Section D — Expense by Category (อ่านได้ แต่ยังไม่แสดง)

เก็บ mapping ไว้ให้ครบตามชีต **ยังไม่มีกราฟ/ตารางบนหน้าเว็บในรอบนี้**

หัวข้อโซน: `D18` = `D. Expense by Category`  
หัวตาราง: `D19` = `Category`, `E19` = `Amount`  
ช่วงข้อมูล: **`D20:E27`**  
แถวรวม: **`D28:E28`** = `TOTAL` / `24,500`

| แถว | เซลล์ชื่อ (D) | เซลล์จำนวน (E) | ค่าตัวอย่าง (E) |
|-----|---------------|-----------------|-----------------|
| 20 | `D20` Gifts | `E20` | 3,300 |
| 21 | `D21` Materials | `E21` | 5,900 |
| 22 | `D22` Venue | `E22` | 0 |
| 23 | `D23` Equipment | `E23` | 8,000 |
| 24 | `D24` Safety | `E24` | 1,200 |
| 25 | `D25` Marketing | `E25` | 2,000 |
| 26 | `D26` Activities | `E26` | 2,800 |
| 27 | `D27` Other | `E27` | 1,300 |
| 28 | `D28` TOTAL | `E28` | 24,500 |

อย่าสับสนกับกราฟขวา: กราฟขวาใช้ Section C (ทีม) ไม่ใช้ Section D

---

### 7.8 Section E — Recent Transactions (อ่านได้ แต่ยังไม่แสดง)

หัวข้อ: `A30` = `E. Recent Transactions`  
หัวตารางแถว 31:

| คอลัมน์ | เซลล์หัว | ความหมาย |
|---------|----------|----------|
| A | `A31` Date | วันที่ (serial ของ Sheets) |
| B | `B31` Team | ทีม |
| C | `C31` Description | รายละเอียด |
| D | `D31` Amount | จำนวนเงิน |

ช่วงข้อมูลเริ่ม **`A32:D32` ลงไป** จนกว่าคอลัมน์ A ว่างทั้งแถว (ปัจจุบันถึงแถว 41)

ตัวอย่างแถวแรก:

| เซลล์ | ค่าดิบ | ค่าหลังแปลง |
|-------|--------|-------------|
| `A32` | `46250` | `2026-08-16` |
| `B32` | `Public Relations Team` | ตามเดิม |
| `C32` | `ค่าออกแบบสื่อออนไลน์` | ตามเดิม |
| `D32` | `500` | `500` |

รอบนี้ไม่เรนเดอร์ตารางรายการบนหน้าเว็บ ถ้าจะเพิ่มในรอบถัดไป ใช้ช่วงนี้ได้เลย โดยแปลงวันที่ตามข้อ 7.3

---

### 7.9 แผนที่เซลล์รวม (สำหรับ implement)

อ่านเฉพาะเซลล์ที่คอลัมน์ UI = ใช่ ในรอบนี้

| เซลล์ | ค่าเมื่อเขียนสเปก | ใช้บนหน้าเว็บรอบนี้ |
|-------|-------------------|----------------------|
| `A1` | งาน Gift โรงเรียนไตรพัฒน์ — เงินกองกลาง | ไม่บังคับ (title สำรอง) |
| `A5` | TOTAL INCOME | ไม่แสดง (ตรวจ label) |
| **`A6`** | 50,016 บาท | **KPI รายรับ** |
| `C5` | TOTAL EXPENSE | ไม่แสดง (ตรวจ label) |
| **`C6`** | 24,500 บาท | **KPI รายจ่าย** |
| `E5` | CURRENT BALANCE | ไม่แสดง (ตรวจ label) |
| **`E6`** | 25,516 บาท | **KPI คงเหลือ** |
| **`A10:B15`** | รายรับรายหมวด | **กราฟซ้าย** |
| `B16` | 50,016 | ตรวจยอด ไม่แสดง |
| **`A20:B25`** | รายจ่ายรายทีม | **กราฟขวา** |
| `B26` | 24,500 | ตรวจยอด ไม่แสดง |
| `D20:E27` | รายจ่ายรายหมวด | ไม่แสดงรอบนี้ |
| `A32:D…` | รายการล่าสุด | ไม่แสดงรอบนี้ |

### 7.10 ค่าที่หน้าเว็บต้องได้ หลัง parse (snapshot สำหรับตรวจ)

ใช้ตรวจตอนเชื่อมชีต ไม่ใช่ค่า hardcode ใน production

| จุดบนหน้า | ค่าที่ต้องเห็น |
|-----------|----------------|
| Total Income | `50,016` |
| Total Expense | `24.5K` |
| Current Balance | `25,516` |
| รวมแท่งกราฟซ้าย | 25,000 + 8,000 + 4,700 + 5,000 + 6,116 + 1,200 = **50,016** |
| รวมแท่งกราฟขวา | 3,300 + 3,900 + 9,200 + 2,000 + 2,700 + 3,400 = **24,500** |

เมื่อทีมการเงินแก้ `02_Income` / `03_Expense` ตัวเลขบนชีตจะเปลี่ยน หน้าเว็บต้องตามเซลล์ ไม่ตามตาราง snapshot นี้

---

## 8. โครงสร้างไฟล์ที่วางไว้ (ยังไม่สร้างโค้ด)

```
Tripat-Dashboard/
├── DESIGN_SPEC.md          ← เอกสารนี้
├── index.html              ← โครงหน้า
├── css/
│   └── styles.css          ← layout, สี, typography, components
├── js/
│   ├── config.js           ← spreadsheet id, gid, cell map
│   ├── sheets.js           ← fetch CSV + parse แถว/คอลัมน์
│   ├── charts.js           ← วาดกราฟแท่ง (canvas หรือ SVG)
│   └── app.js              ← โหลดข้อมูลแล้วเรนเดอร์ KPI + กราฟ
└── assets/                 ← รูปอ้างอิงถ้ามี
```

ข้อกำหนดทางเทคนิค:

- ไม่ใช้ React, Vue, jQuery, Bootstrap, Tailwind
- ไม่ใช้ Google Sheets API client library และไม่มี API key
- กราฟ: วาดเองด้วย Canvas หรือ SVG ไม่ใช้ Chart.js / D3 ในรอบนี้
- ไม่มี build step เปิด `index.html` ในเบราว์เซอร์ได้เลย
- `config.js` ต้องเก็บ cell map เป็นสัญญาเดียวกับข้อ 7 ถ้าชีตเลื่อนแถวในอนาคต แก้ที่นี้ที่เดียว
- ภาษาใน UI ตาม mock: หัวข้อไทย ส่วนในกราฟและการ์ดเป็นอังกฤษ (ชื่อหมวด/ทีมตามชีต)

ข้อจำกัด CORS: ถ้าเปิดไฟล์แบบ `file://` แล้ว `fetch` โดนบล็อก ให้เสิร์ฟด้วย static server ท้องถิ่น (เช่น `python3 -m http.server`)

---

## 9. Accessibility

- การ์ด KPI ใช้ข้อความจริง ไม่ใส่ตัวเลขเป็นภาพ
- contrast ของข้อความบนการ์ดเหลืองและฟ้าต้องผ่าน WCAG AA (ตัวอักษรเข้ม)
- ปุ่มวันที่มี `aria-haspopup`, `aria-expanded`
- กราฟมีตารางข้อมูลซ่อนสำหรับ screen reader (`<table class="sr-only">`) สรุปยอดรายหมวด/ทีม
- โฟกัสเห็นกรอบชัดเมื่อ Tab
- สถานะโหลด/ผิดพลาดต้องเป็นข้อความใน DOM ไม่ใช่สีอย่างเดียว

---

## 10. ลำดับงานพัฒนา (เมื่อเริ่มโค้ด)

ทำทีละขั้น ตรวจกับชีตจริงก่อนไปขั้นถัดไป

1. โครง HTML + CSS ให้ layout ตรงภาพเว็บ (header, 3 การ์ด, 2 ช่องกราฟ) ใส่ `—` ไว้ก่อน
2. `fetch` CSV จาก URL ในข้อ 7.2 ให้ได้ข้อความดิบ
3. parse ตาม cell map ข้อ 7.4–7.6 ให้ KPI เป็น `50,016` / `24.5K` / `25,516`
4. วาดกราฟแท่งจาก `A10:B15` และ `A20:B25` สีตามข้อ 4.3–4.4
5. จัดการ loading / error / parse ล้ม
6. Tooltip, ตรวจจอแคบ, ปุ่มวันที่ตามข้อ 2 (ยังไม่กรอง)

เกณฑ์ผ่านรอบพัฒนา:

- เปิดหน้าแล้วเห็น layout ตรง screenshot เว็บ ที่ความกว้าง ≥ 1280px
- ค่า KPI และแท่งกราฟตรง snapshot ข้อ 7.10 เมื่อชีตยังเป็นชุดข้อมูลชุดนี้
- ไม่มีตัวเลขยอดรวม hardcode ในโค้ด ยกเว้นในเทสต์
- ชีตปิดสิทธิ์สาธารณะแล้วหน้าเว็บแสดง error ไม่แสดงเลขเก่าปลอม
- จอแคบไม่ทำให้การ์ดหรือ legend ล้นจอจนเลื่อนแนวนอนทั้งหน้า

---

## 11. สิ่งที่ตัดสินใจไว้

| หัวข้อ | คำตัดสินในสเปกนี้ |
|--------|-------------------|
| แหล่งข้อมูล | Google Sheets `01_Dashboard` gid `0` อ่านเป็น CSV |
| KPI | `A6`, `C6`, `E6` เท่านั้น ไม่คำนวณ balance เอง |
| กราฟซ้าย | `A10:B15` Income by Source |
| กราฟขวา | `A20:B25` Expense by Team (ชื่อทีมจริง ไม่ใช้หมวดจาก mock เดิม) |
| Section D / E | map เซลล์ไว้แล้ว ไม่แสดงรอบนี้ |
| สกุลเงิน | ไม่แสดง ฿ / บาท บนเว็บ |
| ไลบรารีกราฟ | ไม่อนุญาตในรอบนี้ |
| ตัวกรองวันที่ | ยังไม่ทำ จนกว่าจะอ่าน `02_Income` / `03_Expense` |
| Mock data | ยกเลิก ไม่ฝังยอดใน JS |

เมื่อโครงสร้างชีตเลื่อนแถว ต้องอัปเดตข้อ 7 และ `config.js` ให้ตรงกันก่อนแก้ UI
