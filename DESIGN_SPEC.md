# Design Spec — Gift 2026 Event Finance Dashboard

เอกสารนี้เป็นสเปกสำหรับงานพัฒนา / ปรับปรุง Dashboard หน้าเดียว  
**Tech stack ที่กำหนด:** HTML + CSS + Vanilla JavaScript  
**สถานะ:** ออกแบบสำหรับรอบปรับปรุง ให้ตรงกับชีตและโมเดลกองกลางปัจจุบัน ยังไม่ลงมือเขียนโค้ดในรอบนี้

อ้างอิงต้นทาง (เรียงตามความสำคัญตอน map เซลล์):

1. ชีต `01_Dashboard` ใน `sheets/Gift-Tripat-Finance.xlsx` — สัญญาแถว/คอลัมน์และสูตรจริง
2. `docs/central-fund-finance-model.md` — ความหมายยอด, กฎห้ามปนเงิน 3 ทิศทาง, สิ่งที่หน้าจอต้องสื่อ
3. Screenshot / mock เว็บเดิม — โทนการ์ด + กราฟแท่ง ปรับ layout ตามข้อมูลใหม่

- **หน้าตา:** การ์ด KPI สีทึบ + กราฟแท่ง + ตารางรายการล่าสุด
- **แหล่งข้อมูล:** Google Sheets ชีต `01_Dashboard` เท่านั้น (ไม่ใช้ mock data ใน JS)

---

## 0. สิ่งที่เปลี่ยนจากสเปกเดิม (ต้องแก้ถ้ามีโค้ดแล้ว)

สเปกเวอร์ชันก่อนอิงชีตที่ยังไม่มีคืนเงินและยังไม่มี Shop Revenue **ห้ามใช้ cell map เดิม**

| หัวข้อ | ของเดิม (เลิกใช้) | ของปัจจุบัน |
|--------|-------------------|-------------|
| KPI | 3 ใบ: รายรับ / รายจ่าย / คงเหลือ ที่ `A6`, `C6`, `E6` | **4 ใบ:** รายรับ `A6` · คืนเงิน `C6` · รายจ่าย `E6` · คงเหลือ `G6` |
| `C6` | TOTAL EXPENSE | **TEAM REFUNDS** |
| `E6` | CURRENT BALANCE | **TOTAL EXPENSE** |
| คงเหลือ | `E6` หรือ Income − Expense | **`G6` = Income + Refunds − Expense** ห้ามคำนวณเอง |
| รายรับแยกประเภท | `A10:B15` (6 แหล่ง ไม่มี Shop) | **`A10:B16`** รวม **Shop Revenue** ที่ `A13` รวมที่ `A17:B17` |
| รายจ่ายตามทีม | `A20:B25` คอลัมน์เดียว Total Payment | **`A21:D26`** คอลัมน์ Transferred / Refunded / **Net Spent** รวมที่แถว 27 |
| กราฟขวา | ความสูง = ยอดโอน (B) | ความสูง = **Net Spent (D)** tooltip โชว์โอน/คืน/สุทธิ |
| หมวดรายจ่าย | `D20:E27` | **`F21:G28`** รวมที่ `F29:G29` |
| รายการล่าสุด | `A30` / หัว `A31` / ข้อมูลจาก `A32` | หัวข้อ `A31` หัวตาราง `A32` ข้อมูล **`A33:D42`** |
| ยอดเงินใน `02_Income` | คอลัมน์ E | **คอลัมน์ F** (ชีตมี Workshop Offerings ที่ D) |

---

## 1. วัตถุประสงค์

สร้างหน้า Dashboard การเงินของงาน Gift โรงเรียนไตรพัฒน์ ให้ผู้ดูเห็นภาพรวมเงินกองกลางแบบโปร่งใส ภายในหน้าเดียว

ผู้ใช้หลักคือทีมจัดงาน / PTA / Head ทีมที่**ดูยอดสรุป** ไม่ได้กรอกข้อมูลเอง  
ทีมการเงินเป็น Single Source of Truth บันทึกที่ชีต `02_Income` / `03_Expense` / `08_Refund` แล้ว `01_Dashboard` คำนวณให้

เงินมี 3 ทิศทาง หน้าเว็บต้องไม่ปนความหมาย:

| ทิศทาง | ความหมายบน Dashboard | ชีตต้นทาง |
|--------|----------------------|-----------|
| รายรับ | เงินเข้าจากภายนอก | `02_Income` (`INC-xxxx`) |
| รายจ่าย | กองกลางโอนออก | `03_Expense` (`PAY-xxxx`) |
| คืนเงิน | ทีมส่งเงินทดรองที่เหลือกลับกองกลาง | `08_Refund` (`REF-xxxx`) |

กฎที่หน้าเว็บต้องสื่อให้ถูก:

- **Team Refund ไม่ใช่รายได้** ห้ามรวมในกราฟ Income by Source
- **ยอดใช้จริงของทีม = ยอดโอน − ยอดคืน** ไม่ใช้ยอดโอนอย่างเดียวเป็นแท่งหลัก
- **คงเหลือกองกลาง = รายรับ + คืนเงิน − รายจ่าย** อ่านจาก `G6` ตรง ๆ
- ร้านค้า / เวิร์กช็อป / ประมูล เป็นรายรับ ไม่ใช่คืนเงิน

ไม่มีระบบ login และไม่มี backend ของตัวเองในรอบนี้ — หน้าเว็บดึงชีตสาธารณะฝั่ง client แล้วเรนเดอร์

---

## 2. ขอบเขตงาน (Scope)

### ทำในรอบปรับปรุงนี้

- หน้า Dashboard หน้าเดียว เปิดด้วยไฟล์ HTML โดยตรง
- Header: ชื่องาน Gift โรงเรียนไตรพัฒน์
- การ์ดสรุป **4 ใบ:** รายรับ / คืนเงินจากทีม / รายจ่าย / คงเหลือ — Section A
- กราฟแท่งซ้าย: รายรับแยกประเภท — Section B รวม Shop Revenue **ไม่รวมคืนเงิน**
- กราฟแท่งขวา: รายจ่ายแยกตามทีม — Section C ใช้ **Net Spent**
- กราฟหรือตารางหมวดรายจ่าย — Section D
- ตารางรายการล่าสุด 10 แถว — Section E
- ดึงข้อมูลจาก Google Sheets ชีต `01_Dashboard` ตามข้อ 7
- แปลงข้อความตัวเลขในเซลล์เป็น number ก่อนแสดงผล
- หาช่วงข้อมูลจากหัวข้อเซ็กชันในคอลัมน์ A/F เป็นหลัก แล้วใช้ cell snapshot เป็นค่าตรวจ

### ไม่ทำในรอบนี้

- Backend, database, Google Service Account
- Login / role / permission
- หน้าเพิ่ม-แก้ไขรายการเงิน (กรอกที่ฟอร์ม / ชีตต้นทางเท่านั้น)
- อ่านชีต `02_Income`, `03_Expense`, `04_Team_Budget`, `05_Master_Data`, `08_Refund` โดยตรง
- แยก Workshop Offerings เป็นกราฟย่อย (ID อยู่ใน `02_Income` คอลัมน์ D ยังไม่รวมบน `01_Dashboard`)
- กรองช่วงวันที่ฝั่งเว็บ
- Export PDF / Excel
- Dark mode
- Mobile app
- หน้ารายงาน Refund เต็มรูปแบบ (อยู่ในชีต `09_Refund_Report`)

### ตัวกรองวันที่

ปุ่ม `เลือกช่วงวันที่` **ยังไม่ทำงานในรอบนี้**

เหตุผล: ยอดใน `01_Dashboard` เป็นสรุปทั้งกอง Section A–D ไม่มีวันที่ต่อรายการครบสำหรับกรอง  
ถ้าต้องการกรองวันที่ในรอบถัดไป ให้อ่าน `02_Income`, `03_Expense`, `08_Refund` แล้วคำนวณใหม่

พฤติกรรม UI: แสดงปุ่มไว้ได้ แต่กดแล้วขึ้นข้อความ `แสดงยอดทั้งกองจากชีต Dashboard — ยังไม่กรองวันที่`

---

## 3. โครงสร้างหน้า (Layout)

หน้าเป็นแนวตั้ง กว้างเต็มจอ มี padding รอบด้าน

```
+----------------------------------------------------------------------+
|  งาน Gift โรงเรียนไตรพัฒน์ — เงินกองกลาง          [ เลือกช่วงวันที่ ▾ ] |
+----------------------------------------------------------------------+
|  รายรับ           คืนเงินจากทีม        รายจ่าย            คงเหลือ      |
|  TOTAL INCOME     TEAM REFUNDS        TOTAL EXPENSE      CURRENT     |
|  [    55,200   ]  [     1,150      ]  [    24,500    ]   [  31,850 ] |
+----------------------------------------------------------------------+
|  รายรับแยกประเภท              รายจ่ายแยกตามทีม (ยอดสุทธิหลังหักคืน)   |
|  [ bar: Income by Source ]    [ bar: Net Spent by Team ]             |
+----------------------------------------------------------------------+
|  รายจ่ายแยกหมวด                                                      |
|  [ bar: Expense by Category ]                                        |
+----------------------------------------------------------------------+
|  รายการล่าสุด                                                        |
|  [ table: Date | Team / Source | Description | Amount ]              |
+----------------------------------------------------------------------+
```

ตัวเลขในโครงคือ snapshot จากไฟล์ `Gift-Tripat-Finance.xlsx` ชุดปัจจุบัน **ห้าม hardcode ในโค้ดจริง**

### Grid

| โซน | คอลัมน์ (desktop ≥ 1200px) | ช่องว่าง |
|------|----------------------------|---------|
| Header | 2 คอลัมน์: title ซ้าย / date picker ขวา | — |
| KPI | **4 คอลัมน์เท่ากัน** | 16–24px |
| Charts แถวบน | 2 คอลัมน์เท่ากัน | 24px |
| หมวดรายจ่าย | 1 คอลัมน์เต็มความกว้าง | — |
| ตารางรายการล่าสุด | 1 คอลัมน์เต็มความกว้าง | — |

### ระยะห่าง

| รายการ | ค่า |
|--------|-----|
| Padding หน้า | 32px 40px |
| ระยะระหว่าง header กับ KPI | 28px |
| ระยะระหว่าง KPI กับ charts | 32px |
| ระยะระหว่างแถวกราฟ / ตาราง | 32px |
| ระยะระหว่าง label ภาษาไทยกับกล่อง | 10px |
| Border radius การ์ด / ปุ่ม / กราฟ | 12px |

### Breakpoints

| จอ | พฤติกรรม |
|----|----------|
| ≥ 1200px | ตามโครง: KPI 4 คอลัมน์, กราฟบน 2 คอลัมน์ |
| 768–1199px | KPI 2×2, กราฟเรียงซ้อน 1 คอลัมน์ |
| < 768px | Header ซ้อนกัน, KPI 1 คอลัมน์, กราฟ 1 คอลัมน์, ตารางเลื่อนแนวนอนได้เฉพาะตาราง ไม่เลื่อนทั้งหน้า |

---

## 4. Visual Design

หน้าตาเว็บอ้างอิงโทนการ์ดสีทึบ + กราฟแท่ง ไม่ได้ลอกสีของ Google Sheets ทั้งหน้า แต่สี KPI ให้สื่อทิศทางเงินตามชีต (เขียวรายรับ / ทองคืนเงิน / แดงรายจ่าย / ฟ้าคงเหลือ)

### 4.1 สีพื้นและผิว

| Token | Hex | ใช้กับ |
|-------|-----|--------|
| `--bg-page` | `#F4F1EA` | พื้นหลังหน้า (กระดาษครีม ตามโทนงาน Gift) |
| `--bg-card` | `#FFFFFF` | การ์ดกราฟ, ตาราง, ปุ่ม date picker |
| `--text-primary` | `#1D2433` | หัวข้อหลัก, ตัวเลขเข้ม |
| `--text-secondary` | `#667085` | ป้ายเล็ก, แกนกราฟ, hint |
| `--text-on-dark` | `#FFFFFF` | ข้อความบนการ์ดเขียว / แดง / นำเงิน |
| `--border` | `#E5E7EB` | เส้นขอบปุ่มและตาราง |
| `--grid-line` | `#EEF0F2` | เส้น grid แนวนอนในกราฟ |
| `--shadow` | `0 1px 3px rgba(16,24,40,0.08)` | การ์ด KPI กราฟ และตาราง |
| `--navy` | `#1F4E5F` | หัวข้อ, header ตาราง |
| `--teal` | `#2A6F7F` | การ์ดคงเหลือ, ส่วนเน้น |

### 4.2 สีการ์ด KPI

เรียงซ้ายไปขวาให้ตรงลำดับในชีตแถว 5: Income → Refunds → Expense → Balance

| การ์ด | พื้นหลัง | ข้อความ |
|-------|----------|---------|
| รายรับ (Total Income) | `#2E7D4F` เขียว | ขาว |
| คืนเงินจากทีม (Team Refunds) | `#C4A35A` ทอง | ดำ `#1D2433` |
| รายจ่าย (Total Expense) | `#B85C38` แดงอิฐ | ขาว |
| คงเหลือ (Current Balance) | `#2A6F7F` ฟ้าน้ำทะเล | ขาว |

เหตุผลที่คืนเงินใช้ตัวอักษรเข้ม: พื้นทองตัดกับขาวไม่พอ อ่านยาก

ถ้าคงเหลือติดลบ (รายจ่ายมากกว่าเงินเข้า+คืน) ยังใช้การ์ดใบที่สี่ แต่เพิ่มป้ายเล็ก `ยอดติดลบ` สีขาว 70% ใต้ตัวเลข ไม่เปลี่ยนสูตร

### 4.3 สีแท่งกราฟ — รายรับแยกประเภท (Section B)

เรียงตามลำดับแถวในชีต `A10:A16` ไม่เรียงตามความสูงแท่ง  
**ไม่มีแท่ง Team Refund** ในกราฟนี้

| ลำดับ | หมวด (คอลัมน์ A) | Hex |
|-------|------------------|-----|
| 1 | PTA Contribution | `#3B6EF5` |
| 2 | Sponsorship | `#F5A524` |
| 3 | Workshop Revenue | `#8B5CF6` |
| 4 | Shop Revenue | `#EC4899` |
| 5 | Donations | `#84CC16` |
| 6 | Auction Proceeds | `#22D3EE` |
| 7 | Other Income | `#FACC15` |

ถ้าชีตเพิ่มหมวดใหม่ต่อจากแถวเหล่านี้ ให้ต่อสีจากพาเลตแล้ววนกลับสีแรกเมื่อเกิน 7 สี

### 4.4 สีแท่งกราฟ — รายจ่ายแยกตามทีม (Section C)

เรียงตามลำดับแถว `A21:A26` ความสูงแท่ง = **Net Spent (คอลัมน์ D)**

| ลำดับ | ทีม (คอลัมน์ A) | Hex |
|-------|-----------------|-----|
| 1 | Gift Team | `#3B6EF5` |
| 2 | Shop Team | `#F5A524` |
| 3 | Venue & Safety Team | `#8B5CF6` |
| 4 | Public Relations Team | `#84CC16` |
| 5 | Workshop Team | `#22D3EE` |
| 6 | Activity Team | `#FACC15` |

อย่าใช้ชื่อหมวดค่าใช้จ่าย (Equipment, Materials, …) ในกราฟนี้ หมวดอยู่ Section D

### 4.5 สีแท่งกราฟ — รายจ่ายแยกหมวด (Section D)

เรียงตาม `F21:F28`

| ลำดับ | หมวด (คอลัมน์ F) | Hex |
|-------|------------------|-----|
| 1 | Gifts | `#3B6EF5` |
| 2 | Materials | `#F5A524` |
| 3 | Venue | `#8B5CF6` |
| 4 | Equipment | `#EC4899` |
| 5 | Safety | `#84CC16` |
| 6 | Marketing | `#22D3EE` |
| 7 | Activities | `#FACC15` |
| 8 | Other | `#94A3B8` |

หมวดนี้คำนวณจาก `03_Expense` **ไม่หักคืน** เพราะการคืนผูกกับทีม ไม่ใช่หมวดสินค้า

### 4.6 Typography

ฟอนต์: ใช้ system stack ที่รองรับไทย

```
"Sarabun", "Noto Sans Thai", "Segoe UI", sans-serif
```

โหลด Sarabun จาก Google Fonts (น้ำหนัก 400, 600, 700)

| ระดับ | ขนาด | น้ำหนัก | สี | ใช้กับ |
|-------|------|---------|-----|--------|
| H1 | 24–28px / line 1.2 | 700 | `--navy` | ชื่อ Dashboard |
| Section label | 16px | 600 | `--text-primary` | ป้ายเหนือการ์ดและกราฟ |
| KPI caption | 12–13px | 400 | ตามพื้นการ์ด 70–100% | TOTAL INCOME / TEAM REFUNDS / … |
| KPI value | 28–32px | 700 | ตามพื้นการ์ด | ตัวเลขสรุป |
| Control | 14px | 400 | `--text-primary` | ปุ่มเลือกวันที่ |
| Legend / axis | 12px | 400 | `--text-secondary` | ป้ายกราฟ |
| Table | 13–14px | 400 | `--text-primary` | รายการล่าสุด |

ตัวเลขใช้ tabular lining (`font-variant-numeric: tabular-nums`)

### 4.7 รูปแบบตัวเลข

ค่าที่อ่านจากชีตอาจเป็นข้อความ เช่น `55,200 บาท` ต้อง parse เป็น number ก่อน แล้วค่อยจัดรูปแบบตอนแสดงผล

| จุดแสดง | รูปแบบ | ตัวอย่าง snapshot |
|---------|--------|-------------------|
| การ์ด KPI ทั้ง 4 ใบ | `#,###` เต็มจำนวน | `55,200` / `1,150` / `24,500` / `31,850` |
| ตารางรายการล่าสุด | `#,###` | `2,200` |
| แกน Y ของกราฟ | `0`, `10K`, `20K` หรือ `0`, `2K`, `4K` | ตามสเกลชุดข้อมูล |
| Tooltip | `#,###` เต็มจำนวน | `25,000` |

สกุลเงินบนหน้าเว็บ **ไม่แสดง ฿ และไม่แสดงคำว่า บาท** (แม้เซลล์ในชีตจะมีคำว่า บาท)

ไม่ย่อเป็น `24.5K` บนการ์ดในรอบนี้ เพื่อให้อ่านยอดการเงินชัด

---

## 5. Components

### 5.1 Header

- ซ้าย: `งาน Gift โรงเรียนไตรพัฒน์ — เงินกองกลาง`  
  อ่านจาก `A1` ได้ ถ้า `A1` ว่างใช้ข้อความนี้เป็นค่าเริ่ม
- ขวา: ปุ่ม `เลือกช่วงวันที่` พื้นขาว ขอบ `#E5E7EB` มุมโค้ง 8–12px สูงประมาณ 40px มีลูกศรชี้ลง
- คลิกแล้วแสดงข้อความตามข้อ 2 ไม่เปิด date picker จริงในรอบนี้
- ใต้ H1 ใส่ subtitle เล็กจาก `A2` ได้ (ตัดให้สั้นถ้ายาวเกินหนึ่งบรรทัด)

### 5.2 KPI Card

แต่ละใบมี 2 ส่วน:

1. **Label ด้านนอก** ภาษาไทย อยู่เหนือกล่องสี
2. **กล่องสี** เต็มความกว้างคอลัมน์ สูงประมาณ 88–96px padding 16px 20px
   - บรรทัดบน: caption ภาษาอังกฤษตามชีตแถว 5
   - บรรทัดล่าง: ตัวเลขใหญ่จากแถว 6

สี่ใบ: รายรับ → คืนเงินจากทีม → รายจ่าย → คงเหลือ

| UI ไทย | Caption ในกล่อง | เซลล์ค่า |
|--------|-----------------|----------|
| รายรับ | TOTAL INCOME | `A6` |
| คืนเงินจากทีม | TEAM REFUNDS | `C6` |
| รายจ่าย | TOTAL EXPENSE | `E6` |
| คงเหลือ | CURRENT BALANCE | `G6` |

**ห้ามคำนวณ balance เอง** ให้ใช้ `G6` จากชีต  
ถ้า parse ไม่ได้ ให้แสดง `—`

การ์ดในชีตกินคอลัมน์คู่ (A–B, C–D, E–F, G–H) ค่าที่ export CSV อยู่ที่ **A6, C6, E6, G6** อย่าอ่าน B6 / D6 / F6 / H6

### 5.3 Chart Card

กล่องขาว มีเงาบาง มุม 12px padding 20px 24px

โครงสร้างภายในจากบนลงล่าง:

1. หัวข้อภาษาไทย
2. Legend แนวนอน ชิดซ้าย ไอคอนสี่เหลี่ยม 10×10px ตามด้วยชื่อ ระยะห่าง 16px ถ้าไม่พอให้ขึ้นบรรทัดใหม่
3. พื้นที่กราฟแท่งแนวตั้ง

กราฟ:

- แท่งแนวตั้ง สีต่างกันตามหมวด/ทีม ไม่มีขอบ
- ความกว้างแท่งประมาณ 28–48px (กราฟรายรับมี 7 แท่ง กราฟหมวดมี 8 แท่ง ให้แคบลงได้)
- มุมบนของแท่งไม่โค้ง
- เส้น grid แนวนอนบาง สี `--grid-line` ไม่มีเส้นแกนแนวตั้ง
- แกน Y ชิดซ้าย หน่วยย่อเป็น K
- แกน X เป็นชื่อจากชีต ถ้าชื่อยาวให้ตัดบรรทัดหรือเอียงเล็กน้อย
- ไม่มีค่าตัวเลขบนยอดแท่งในสถานะปกติ ค่าเต็มอยู่ใน tooltip
- แถวที่ชื่อเป็น `TOTAL` **ห้ามวาดเป็นแท่ง**
- แถวที่จำนวนเป็น 0 ยังวาดแท่งสูง 0 และยังอยู่ใน legend

สเกลแกน Y: ปัดขึ้นเป็นจำนวนกลมที่อ่านง่าย ตามค่าสูงสุดของชุดข้อมูลที่อ่านได้

หัวข้อกราฟขวาต้องบอกว่าเป็นยอดสุทธิ เช่น `รายจ่ายแยกตามทีม` และคำอธิบายเล็ก `ยอดใช้จริงหลังหักเงินคืน`

### 5.4 ตารางรายการล่าสุด

กล่องขาว มุม 12px หัวตารางพื้น `--navy` ตัวอักษรขาว

| คอลัมน์เว็บ | คอลัมน์ชีต | หมายเหตุ |
|-------------|------------|----------|
| วันที่ | A | แปลง serial เป็น `DD/MM/YYYY` |
| ทีม / แหล่ง | B | หัวในชีตคือ `Team / Source` รายรับเป็นชื่อแหล่ง รายจ่าย/คืนเป็นชื่อทีม |
| รายละเอียด | C | ตามชีต |
| จำนวน | D | `#,###` ชิดขวา |

ชีต **ไม่มีคอลัมน์ Type** และจำนวนไม่ได้ใส่เครื่องหมาย +/−  
รอบนี้แสดงตามชีต ไม่เดาว่าเป็นรายรับหรือคืนเงินจากคำในรายละเอียด

เรียงตามลำดับแถวในชีต (รายการใหม่กว่าอยู่บนใน snapshot ปัจจุบัน) หยุดเมื่อคอลัมน์ A ว่าง หรือครบ 10 แถวที่มีข้อมูล

แถวสลับสีพื้น `#F7F4EE` ตามชีตได้

### 5.5 Empty / loading / error

| สถานะ | พฤติกรรม |
|--------|----------|
| กำลังดึงชีต | การ์ด กราฟ ตารางแสดงโครง ตัวเลขเป็น `—` มีข้อความเล็ก `กำลังโหลดข้อมูล` |
| ดึงสำเร็จ | เรนเดอร์ตาม cell map / section finder |
| ชีตไม่สาธารณะ / CORS / เครือข่ายล้ม | แสดงข้อความ `ไม่สามารถอ่าน Google Sheets ได้` ไม่ใส่ตัวเลขปลอม |
| เซลล์ว่างหรือ parse ไม่ได้ | ช่องนั้นเป็น `—` หรือแท่ง 0 ตามบริบท |
| CSV มีแถวน้อยกว่าที่คาด | ข้ามแถวที่ไม่มี ไม่ทำให้ทั้งหน้าพัง |
| หาหัวข้อเซ็กชันไม่เจอ | fallback ไป cell snapshot ข้อ 7 ถ้ายังไม่ได้ ให้แสดง error ของเซ็กชันนั้น ไม่พังทั้งหน้า |

---

## 6. Interaction

ทั้งหมดทำงานฝั่ง client ไม่รีเฟรชหน้า

### 6.1 เลือกช่วงวันที่

รอบนี้ยังไม่กรองข้อมูล ดูข้อ 2

### 6.2 Tooltip กราฟ

เมื่อชี้แท่ง:

- กล่องเล็กพื้นขาว เงา ขอบบาง
- กราฟรายรับ / หมวด: ชื่อ + จำนวนเต็มมี comma
- กราฟทีม: 3 บรรทัด
  - ทีม
  - โอน (Transferred)
  - คืน (Refunded)
  - สุทธิ (Net Spent) — ค่าเดียวกับความสูงแท่ง
- ไม่คลิกแท่งแล้วทำอะไรเพิ่มในรอบนี้

### 6.3 Legend

legend เป็นป้ายอ่านอย่างเดียว ไม่กดซ่อนซีรีส์

### 6.4 Keyboard

- Tab ไปที่ปุ่มวันที่ได้
- Escape ปิดข้อความ/popover ของปุ่มวันที่

### 6.5 Refresh ข้อมูล

ไม่มีปุ่มรีเฟรชในรอบนี้ โหลดชีตครั้งเดียวตอนเปิดหน้า  
ถ้าต้องการข้อมูลใหม่ ผู้ใช้รีโหลดหน้าเว็บ

---

## 7. แหล่งข้อมูล — Google Sheets

### 7.1 สมุดงาน

| รายการ | ค่า |
|--------|-----|
| ชื่อไฟล์ | Gift-Tripat-Finance |
| Spreadsheet ID | `1OQ8fC-vOglnPcy3aQKXOu743dX8YK2f9OovYg_8EjlA` |
| URL แก้ไข | [เปิดชีต](https://docs.google.com/spreadsheets/d/1OQ8fC-vOglnPcy3aQKXOu743dX8YK2f9OovYg_8EjlA/edit?usp=sharing) |
| ไฟล์ต้นแบบใน repo | `sheets/Gift-Tripat-Finance.xlsx` |
| ชีตที่ใช้ในรอบนี้ | `01_Dashboard` |
| gid ของ `01_Dashboard` | `0` |
| สิทธิ์ที่ต้องตั้ง | Anyone with the link → Viewer (ไม่งั้น `fetch` จากเบราว์เซอร์ไม่ได้) |

แท็บอื่นในสมุดงานเดียวกัน **ห้ามดึงในรอบนี้** แต่บันทึกไว้เพื่อเข้าใจสูตรบน Dashboard:

| ชีต | บทบาท | หน้าเว็บรอบนี้ |
|------|--------|----------------|
| `01_Dashboard` | ยอดสรุปที่หน้าเว็บอ่าน | อ่าน |
| `02_Income` | รายรับภายนอก (`INC-xxxx`) คอลัมน์ Amount = **F** | ไม่อ่าน |
| `03_Expense` | รายจ่าย (`PAY-xxxx`) Amount = F | ไม่อ่าน |
| `04_Team_Budget` | งบต่อทีม หักคืนแล้ว | ไม่อ่าน |
| `05_Master_Data` | dropdown รวม Workshop Offerings (F–G) | ไม่อ่าน |
| `06_Income_Report` | รายงานรายรับ | ไม่อ่าน |
| `07_Expense_Report` | รายงานรายจ่าย | ไม่อ่าน |
| `08_Refund` | คืนเงิน (`REF-xxxx`) Amount = E | ไม่อ่าน |
| `09_Refund_Report` | รายงานคืนเงิน | ไม่อ่าน |

สูตรบน `01_Dashboard` ที่หน้าเว็บต้องเข้าใจ (ไม่ต้องคำนวณซ้ำ):

```
Total Income     = SUM(02_Income Amount F)     // ไม่รวมเงินคืน
Team Refunds     = SUM(08_Refund Amount E)
Total Expense    = SUM(03_Expense Amount F)    // ยอดโอนออกทั้งหมด ไม่ลบคืนในแถวนี้
Current Balance  = Total Income + Team Refunds - Total Expense

Income by Source Amount = SUMIF(02_Income Source, name, Amount F)
Team Transferred        = SUMIF(03_Expense Team, name, Amount F)
Team Refunded           = SUMIF(08_Refund Team, name, Amount E)
Team Net Spent          = Transferred - Refunded
Category Amount         = SUMIF(03_Expense Category, name, Amount F)  // ไม่หักคืน
```

### 7.2 วิธีดึงข้อมูล (Vanilla JS)

ดึง CSV ของชีต `01_Dashboard` แล้ว map ตามแถว/คอลัมน์ด้านล่าง ไม่ใช้ Google Sheets API key

URL ที่ใช้:

```
https://docs.google.com/spreadsheets/d/1OQ8fC-vOglnPcy3aQKXOu743dX8YK2f9OovYg_8EjlA/gviz/tq?tqx=out:csv&gid=0
```

ทางเลือกสำรองถ้า URL ด้านบนล้ม:

```
https://docs.google.com/spreadsheets/d/1OQ8fC-vOglnPcy3aQKXOu743dX8YK2f9OovYg_8EjlA/export?format=csv&gid=0
```

ข้อกำหนดการ parse:

- แถวที่ 1 ของ CSV = แถวที่ 1 ของชีต (A1)
- คอลัมน์ A = index 0, B = 1, C = 2, D = 3, E = 4, F = 5, G = 6, H = 7
- แผนที่ด้านล่างใช้ **แถว/คอลัมน์แบบ Google Sheets (1-based)** เช่น `B10` = แถว 10 คอลัมน์ B
- เซลล์ที่ merge แล้ว ค่า CSV อยู่ที่เซลล์ซ้ายบนของช่วง merge
- **หาเซ็กชันจากข้อความหัวข้อก่อน** แล้วค่อย fallback ไปแถวคงที่ในข้อ 7.4–7.8 เพราะถ้าเพิ่ม Income Source ใน Master Data แถวด้านล่างจะเลื่อน
- ข้ามแถวที่คอลัมน์ชื่อว่าง
- ข้ามแถวที่ชื่อเท่ากับ `TOTAL` (ไม่สนตัวพิมพ์) เมื่อสร้างกราฟ

ตัวหาเซ็กชัน (คอลัมน์ A ยกเว้นหมวดรายจ่ายอยู่คอลัมน์ F):

| หัวข้อที่ค้น | ความหมาย |
|--------------|----------|
| `A. Financial Overview` | KPI อยู่ 2 แถวถัดไป |
| `B. Income by Source` | ตารางแหล่งรายรับ แถวถัดไปเป็นหัว `Source` / `Amount` แล้ววนจน `TOTAL` |
| `C. Expense by Team` | ตารางทีม แถวถัดไปเป็นหัว `Team` / `Transferred` / `Refunded` / `Net Spent` |
| `D. Expense by Category` | อยู่คอลัมน์ F ของแถวเดียวกับหัวข้อ C |
| `E. Recent Transactions` | แถวถัดไปเป็นหัวตาราง แล้ววนจนคอลัมน์ A ว่างหรือเจอข้อความขึ้นต้นด้วย `ตารางนี้เป็นข้อมูลตัวอย่าง` / `วิธีใช้งาน` |

### 7.3 การแปลงค่าในเซลล์

| รูปแบบในเซลล์ | วิธีแปลง | ผล |
|---------------|----------|-----|
| `55,200 บาท` | ลบ `บาท`, ลบ comma, `trim`, `Number(...)` | `55200` |
| `24,500` | ลบ comma | `24500` |
| `0` | number | `0` |
| ว่าง | ไม่มีค่า | `null` → แสดง `—` หรือข้ามแถว |
| serial วันที่ เช่น `46257` | Google serial (epoch `1899-12-30`) | `2026-08-23` |
| วันที่ ISO / `YYYY-MM-DD` จาก CSV | parse เป็นวันที่ | แสดง `DD/MM/YYYY` |

สูตรวันที่:

```
date = 1899-12-30 + serial วัน
ตัวอย่าง: 46250 → 2026-08-16
```

ห้ามแสดง serial ดิบบนหน้าเว็บ

---

### 7.4 Section A — Financial Overview (KPI)

ช่วงที่เกี่ยวข้อง: แถว **4–6** คอลัมน์ **A, C, E, G**

| UI | Label ในชีต | เซลล์ label | เซลล์ค่า | สูตรในชีต | ค่า snapshot |
|----|-------------|-------------|----------|-----------|--------------|
| การ์ดรายรับ | TOTAL INCOME | `A5` | `A6` | `SUM('02_Income'!F2:F1000)` | `55,200 บาท` |
| การ์ดคืนเงิน | TEAM REFUNDS | `C5` | `C6` | `SUM('08_Refund'!E2:E1000)` | `1,150 บาท` |
| การ์ดรายจ่าย | TOTAL EXPENSE | `E5` | `E6` | `SUM('03_Expense'!F2:F1000)` | `24,500 บาท` |
| การ์ดคงเหลือ | CURRENT BALANCE | `G5` | `G6` | `=A6+C6-E6` | `31,850 บาท` |

เซลล์อ้างอิง (ไม่ต้องแสดง แต่ใช้ตรวจ / subtitle):

| เซลล์ | เนื้อหา |
|-------|---------|
| `A1` | `งาน Gift โรงเรียนไตรพัฒน์ — เงินกองกลาง` |
| `A2` | `Real-time Dashboard  \|  ทีมการเงินเป็น Single Source of Truth  \|  รายรับ 02_Income  รายจ่าย 03_Expense  คืนเงิน 08_Refund` |
| `A4` | `A. Financial Overview` |

```
แถว 5:  A5 TOTAL INCOME     C5 TEAM REFUNDS     E5 TOTAL EXPENSE     G5 CURRENT BALANCE
แถว 6:  A6 55,200 บาท       C6 1,150 บาท        E6 24,500 บาท        G6 31,850 บาท
```

ตรวจทางเลือก: `A6 + C6 - E6` ควรเท่ากับ `G6` — ถ้าไม่เท่า ยังแสดง `G6` ตามชีต อย่าแทนที่ด้วยผลคำนวณฝั่งเว็บ

---

### 7.5 Section B — Income by Source → กราฟซ้าย

ช่วงข้อมูล: **`A10:B16`**  
หัวตาราง: `A9` = `Source`, `B9` = `Amount`  
หัวข้อโซน: `A8` = `B. Income by Source`  
แถวรวม: **`A17:B17`** ชื่อ `TOTAL` — ใช้ตรวจยอด ไม่วาดแท่ง

| แถว | เซลล์ชื่อ (A) | เซลล์จำนวน (B) | ค่า snapshot (B) |
|-----|---------------|-----------------|------------------|
| 10 | PTA Contribution | `B10` | 25,000 |
| 11 | Sponsorship | `B11` | 8,000 |
| 12 | Workshop Revenue | `B12` | 8,500 |
| 13 | Shop Revenue | `B13` | 3,500 |
| 14 | Donations | `B14` | 3,000 |
| 15 | Auction Proceeds | `B15` | 6,000 |
| 16 | Other Income | `B16` | 1,200 |
| 17 | TOTAL | `B17` | 55,200 |

Workshop Revenue ใน snapshot = INC-0004 (4,500) + INC-0008 (1,800) + INC-0009 (2,200)  
Shop Revenue = INC-0007 (3,500)

กฎอ่าน:

1. เริ่มแถวหลังหัว `Source` / `Amount`
2. ถ้า `A{n}` ว่าง ให้ข้าม
3. ถ้า `A{n}` เป็น `TOTAL` ให้หยุด และเก็บ `B{n}` ไว้ตรวจยอด
4. parse `B{n}` เป็น number แล้วใช้เป็นความสูงแท่ง
5. ตรวจทางเลือก: ผลรวมแท่งควรเท่ากับแถว TOTAL และใกล้ `A6` — ถ้าไม่เท่า ยังเรนเดอร์กราฟจากแถวข้อมูล แต่ไม่ overwrite การ์ด KPI
6. ถ้าเจอชื่อคล้าย `Team Refund` / `Refund` ให้**ข้าม** ไม่วาดแท่ง (โมเดลห้ามนับคืนเงินเป็นรายได้)

---

### 7.6 Section C — Expense by Team → กราฟขวา

ช่วงข้อมูล: **`A21:D26`**  
หัวข้อโซน: `A19` = `C. Expense by Team`  
หัวตารางแถว **20**:

| คอลัมน์ | หัว | ความหมาย | ใช้บนกราฟ |
|---------|-----|----------|-----------|
| A | Team | ชื่อทีม | แกน X / legend |
| B | Transferred | ยอดโอนออกจากกองกลาง | tooltip |
| C | Refunded | ยอดที่ทีมคืน | tooltip |
| D | Net Spent | ยอดใช้จริง = B − C | **ความสูงแท่ง** |

แถวรวม: **`A27:D27`** ชื่อ `TOTAL`

| แถว | ทีม | Transferred (B) | Refunded (C) | Net Spent (D) |
|-----|-----|-----------------|--------------|---------------|
| 21 | Gift Team | 3,300 | 150 | 3,150 |
| 22 | Shop Team | 3,900 | 400 | 3,500 |
| 23 | Venue & Safety Team | 9,200 | 0 | 9,200 |
| 24 | Public Relations Team | 2,000 | 200 | 1,800 |
| 25 | Workshop Team | 2,700 | 300 | 2,400 |
| 26 | Activity Team | 3,400 | 100 | 3,300 |
| 27 | TOTAL | 24,500 | 1,150 | 23,350 |

กฎอ่าน:

1. วนแถวข้อมูลจนชื่อเป็น `TOTAL`
2. ความสูงแท่ง = Net Spent (D) ไม่ใช่ Transferred (B)
3. ตรวจทางเลือก: รวม B ควรใกล้ `E6` รวม C ควรใกล้ `C6` รวม D = รวม B − รวม C
4. แถวที่ D = 0 ยังอยู่ใน legend

อย่าสับสนกับกราฟหมวด: กราฟนี้ใช้ชื่อทีมคอลัมน์ A ไม่ใช้คอลัมน์ F

---

### 7.7 Section D — Expense by Category → กราฟแถวล่าง

หัวข้อโซน: `F19` = `D. Expense by Category`  
หัวตาราง: `F20` = `Category`, `G20` = `Amount`  
ช่วงข้อมูล: **`F21:G28`**  
แถวรวม: **`F29:G29`** = `TOTAL` / `24,500`

ตารางนี้เริ่มแถวเดียวกับตารางทีม แต่**ยาวกว่า** เพราะมี 8 หมวด (ทีมมี 6 แถว + TOTAL ที่แถว 27 ส่วนหมวด TOTAL อยู่แถว 29)

| แถว | เซลล์ชื่อ (F) | เซลล์จำนวน (G) | ค่า snapshot (G) |
|-----|---------------|-----------------|------------------|
| 21 | Gifts | `G21` | 3,300 |
| 22 | Materials | `G22` | 5,900 |
| 23 | Venue | `G23` | 0 |
| 24 | Equipment | `G24` | 8,000 |
| 25 | Safety | `G25` | 1,200 |
| 26 | Marketing | `G26` | 2,000 |
| 27 | Activities | `G27` | 2,800 |
| 28 | Other | `G28` | 1,300 |
| 29 | TOTAL | `G29` | 24,500 |

รวมหมวดควรใกล้ TOTAL EXPENSE (`E6`) ไม่ใกล้ Net Spent ของทีม เพราะหมวดไม่หักคืน

---

### 7.8 Section E — Recent Transactions → ตารางล่าง

หัวข้อ: `A31` = `E. Recent Transactions`  
หัวตารางแถว **32**:

| คอลัมน์ | เซลล์หัว | ความหมาย |
|---------|----------|----------|
| A | Date | วันที่ |
| B | Team / Source | ทีม (รายจ่าย/คืน) หรือแหล่งรายรับ |
| C | Description | รายละเอียด |
| D | Amount | จำนวนเงิน (ไม่มีเครื่องหมาย) |

ช่วงข้อมูล: **`A33:D42`** (10 แถวใน snapshot) หยุดเมื่อ A ว่าง หรือเจอโน้ตแถว 44

ตัวอย่าง snapshot (เรียงใหม่กว่าขึ้นก่อน ตามไฟล์ xlsx):

| แถว | วันที่ | Team / Source | Description | Amount |
|-----|--------|---------------|-------------|--------|
| 33 | 23/08/2026 | Activity Team | คืนเงินสดเหลือค่าขนส่งอุปกรณ์กิจกรรม | 100 |
| 34 | 22/08/2026 | Gift Team | คืนเงินสดเหลือค่าของที่ระลึกผู้บริจาค | 150 |
| 35 | 21/08/2026 | Public Relations Team | คืนเงินเหลือค่าพิมพ์โปสเตอร์ | 200 |
| 36 | 21/08/2026 | Shop Team | คืนเงินเหลือค่าสินค้าบูธร้านค้า | 400 |
| 37 | 20/08/2026 | Workshop Team | คืนเงินเหลือค่าวัสดุงานฝีมือ Workshop | 300 |
| 38 | 18/08/2026 | Workshop Revenue | รายได้จาก Workshop สติมา Tea (ชาภาวนา) | 2,200 |
| 39 | 17/08/2026 | Workshop Revenue | รายได้จาก Workshop เย็บสมุด | 1,800 |
| 40 | 16/08/2026 | Public Relations Team | ค่าออกแบบสื่อออนไลน์ | 500 |
| 41 | 16/08/2026 | Shop Revenue | รายได้จากบูธร้านค้างาน Gift | 3,500 |
| 42 | 15/08/2026 | Shop Team | ถุงกระดาษสำหรับขายของ | 700 |

หลังอัปโหลด Google Sheets แล้วรันเมนู **สร้างชีตรายงาน** สูตรที่ `A32` อาจดึง 10 รายการล่าสุดอัตโนมัติ หน้าเว็บอ่านค่าที่เห็นไม่สนว่าเป็นค่าคงที่หรือสูตร

อย่าอ่านแถวโน้ต `A44` และ `วิธีใช้งาน` `A45:A46` เป็นรายการเงิน

---

### 7.9 แผนที่เซลล์รวม (สำหรับ implement)

อ่านเฉพาะเซลล์ที่คอลัมน์ UI = ใช่ ในรอบนี้

| เซลล์ | ค่า snapshot | ใช้บนหน้าเว็บรอบนี้ |
|-------|--------------|----------------------|
| `A1` | งาน Gift โรงเรียนไตรพัฒน์ — เงินกองกลาง | H1 |
| `A2` | คำอธิบายแหล่งข้อมูล | subtitle ไม่บังคับ |
| **`A6`** | 55,200 บาท | **KPI รายรับ** |
| **`C6`** | 1,150 บาท | **KPI คืนเงินจากทีม** |
| **`E6`** | 24,500 บาท | **KPI รายจ่าย** |
| **`G6`** | 31,850 บาท | **KPI คงเหลือ** |
| **`A10:B16`** | รายรับ 7 แหล่ง | **กราฟซ้าย** |
| `B17` | 55,200 | ตรวจยอด ไม่แสดง |
| **`A21:D26`** | ทีม โอน/คืน/สุทธิ | **กราฟขวา (สูง = D)** |
| `B27` `C27` `D27` | 24,500 / 1,150 / 23,350 | ตรวจยอด ไม่แสดง |
| **`F21:G28`** | รายจ่ายรายหมวด | **กราฟแถวล่าง** |
| `G29` | 24,500 | ตรวจยอด ไม่แสดง |
| **`A33:D42`** | รายการล่าสุด | **ตารางล่าง** |

### 7.10 ค่าที่หน้าเว็บต้องได้ หลัง parse (snapshot สำหรับตรวจ)

ใช้ตรวจตอนเชื่อมชีตชุด `Gift-Tripat-Finance.xlsx` ปัจจุบัน ไม่ใช่ค่า hardcode ใน production

| จุดบนหน้า | ค่าที่ต้องเห็น |
|-----------|----------------|
| Total Income | `55,200` |
| Team Refunds | `1,150` |
| Total Expense | `24,500` |
| Current Balance | `31,850` |
| รวมแท่งกราฟซ้าย | 25,000 + 8,000 + 8,500 + 3,500 + 3,000 + 6,000 + 1,200 = **55,200** |
| รวมแท่งกราฟขวา (Net Spent) | 3,150 + 3,500 + 9,200 + 1,800 + 2,400 + 3,300 = **23,350** |
| รวมแท่งกราฟหมวด | 3,300 + 5,900 + 0 + 8,000 + 1,200 + 2,000 + 2,800 + 1,300 = **24,500** |
| แถวแรกของตาราง | 23/08/2026 · Activity Team · 100 |

ตรวจความสัมพันธ์ (อย่าใช้ทับค่าชีต ใช้แค่เตือนตอนพัฒนา):

```
Income KPI              == sum(Income by Source)
Expense KPI             == sum(Expense by Category)
Refunds KPI             == sum(Team Refunded)
Balance KPI             == Income + Refunds - Expense
sum(Team Transferred)   == Expense KPI
sum(Team Net Spent)     == Expense KPI - Refunds KPI
```

เมื่อทีมการเงินแก้ `02_Income` / `03_Expense` / `08_Refund` ตัวเลขบนชีตจะเปลี่ยน หน้าเว็บต้องตามเซลล์ ไม่ตามตาราง snapshot นี้

---

## 8. โครงสร้างไฟล์ที่วางไว้ (ยังไม่สร้างโค้ด)

```
Tripat-Dashboard/
├── DESIGN_SPEC.md          ← สำเนาหรือลิงก์มาจาก docs/DESIGN_SPEC.md
├── index.html              ← โครงหน้า
├── css/
│   └── styles.css          ← layout, สี, typography, components
├── js/
│   ├── config.js           ← spreadsheet id, gid, section labels, cell fallback
│   ├── sheets.js           ← fetch CSV + parse แถว/คอลัมน์ + หาเซ็กชัน
│   ├── charts.js           ← วาดกราฟแท่ง (canvas หรือ SVG)
│   └── app.js              ← โหลดข้อมูลแล้วเรนเดอร์ KPI + กราฟ + ตาราง
└── assets/                 ← รูปอ้างอิงถ้ามี
```

ใน repo นี้เอกสารสเปกอยู่ที่ `docs/DESIGN_SPEC.md`

ข้อกำหนดทางเทคนิค:

- ไม่ใช้ React, Vue, jQuery, Bootstrap, Tailwind
- ไม่ใช้ Google Sheets API client library และไม่มี API key
- กราฟ: วาดเองด้วย Canvas หรือ SVG ไม่ใช้ Chart.js / D3 ในรอบนี้
- ไม่มี build step เปิด `index.html` ในเบราว์เซอร์ได้เลย
- `config.js` เก็บทั้ง section labels และ cell fallback ให้ตรงข้อ 7 ถ้าชีตเลื่อนแถว แก้ที่นี้ที่เดียว
- ภาษาใน UI: หัวข้อไทย caption ในกล่องเป็นอังกฤษตามชีต ชื่อหมวด/ทีมตามชีต

ข้อจำกัด CORS: ถ้าเปิดไฟล์แบบ `file://` แล้ว `fetch` โดนบล็อก ให้เสิร์ฟด้วย static server ท้องถิ่น (เช่น `python3 -m http.server`)

---

## 9. Accessibility

- การ์ด KPI ใช้ข้อความจริง ไม่ใส่ตัวเลขเป็นภาพ
- contrast ของข้อความบนการ์ดทองต้องผ่าน WCAG AA (ตัวอักษรเข้ม)
- ปุ่มวันที่มี `aria-haspopup`, `aria-expanded`
- กราฟมีตารางข้อมูลซ่อนสำหรับ screen reader (`<table class="sr-only">`) สรุปยอดรายหมวด/ทีม รวม Transferred / Refunded / Net ของทีม
- โฟกัสเห็นกรอบชัดเมื่อ Tab
- สถานะโหลด/ผิดพลาดต้องเป็นข้อความใน DOM ไม่ใช่สีอย่างเดียว

---

## 10. ลำดับงานพัฒนา (เมื่อเริ่มโค้ด)

ทำทีละขั้น ตรวจกับชีตจริงก่อนไปขั้นถัดไป

1. โครง HTML + CSS: header, **4 การ์ด**, กราฟ 2 ช่อง, กราฟหมวด, ตาราง ใส่ `—` ไว้ก่อน
2. `fetch` CSV จาก URL ในข้อ 7.2 ให้ได้ข้อความดิบ
3. parse KPI ข้อ 7.4 ให้ได้ `55,200` / `1,150` / `24,500` / `31,850`
4. วาดกราฟรายรับจาก `A10:B16` สีตามข้อ 4.3 รวม Shop Revenue
5. วาดกราฟทีมจาก `A21:D26` สูงด้วยคอลัมน์ D สีตามข้อ 4.4 tooltip มีโอน/คืน/สุทธิ
6. วาดกราฟหมวดจาก `F21:G28`
7. ตารางรายการล่าสุดจาก `A33:D…` แปลงวันที่ตามข้อ 7.3
8. ตัวหาเซ็กชันตามหัวข้อ แล้วค่อย fallback แถวคงที่
9. loading / error / parse ล้ม
10. Tooltip, จอแคบ, ปุ่มวันที่ตามข้อ 2 (ยังไม่กรอง)

เกณฑ์ผ่านรอบพัฒนา:

- เปิดหน้าแล้วเห็น layout ตามข้อ 3 ที่ความกว้าง ≥ 1280px
- ค่า KPI และแท่งกราฟตรง snapshot ข้อ 7.10 เมื่อชีตยังเป็นชุดข้อมูลชุดนี้
- การ์ดคงเหลือตรง `G6` ไม่ใช่ `E6` และไม่ใช่ Income − Expense
- กราฟรายรับมี Shop Revenue และ**ไม่มี**แท่งคืนเงิน
- กราฟทีมใช้ Net Spent รวม 23,350 ไม่ใช่ยอดโอน 24,500
- ไม่มีตัวเลขยอดรวม hardcode ในโค้ด ยกเว้นในเทสต์
- ชีตปิดสิทธิ์สาธารณะแล้วหน้าเว็บแสดง error ไม่แสดงเลขเก่าปลอม
- จอแคบไม่ทำให้การ์ดหรือ legend ล้นจอจนเลื่อนแนวนอนทั้งหน้า

---

## 11. สิ่งที่ตัดสินใจไว้

| หัวข้อ | คำตัดสินในสเปกนี้ |
|--------|-------------------|
| แหล่งข้อมูล | Google Sheets `01_Dashboard` gid `0` อ่านเป็น CSV |
| KPI | `A6`, `C6`, `E6`, `G6` ไม่คำนวณ balance เอง |
| กราฟซ้าย | `A10:B16` Income by Source รวม Shop Revenue ไม่รวม Refund |
| กราฟขวา | `A21:D26` ความสูง = Net Spent (D) |
| กราฟหมวด | `F21:G28` แสดงในรอบนี้ ไม่หักคืน |
| ตารางล่าง | `A33:D…` Recent Transactions แสดงในรอบนี้ |
| Workshop Offerings | ยังไม่แยกกราฟ รายได้ Workshop รวมในแท่ง Workshop Revenue |
| สกุลเงิน | ไม่แสดง ฿ / บาท บนเว็บ |
| ไลบรารีกราฟ | ไม่อนุญาตในรอบนี้ |
| ตัวกรองวันที่ | ยังไม่ทำ จนกว่าจะอ่านชีตรายการต้นทาง |
| Mock data | ยกเลิก ไม่ฝังยอดใน JS |

เมื่อโครงสร้างชีตเลื่อนแถว ต้องอัปเดตข้อ 7 และ `config.js` ให้ตรงกันก่อนแก้ UI โดยพึ่งตัวหาเซ็กชันจากหัวข้อเป็นหลัก
